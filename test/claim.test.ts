describe('StakingPool.claim reward', () => {
  context('when user reward is 0', async () => {
    it('reverts if user reward is 0');
  });

  context('when user reward is not 0', async () => {
    it('success', async () => {});
  });
});
